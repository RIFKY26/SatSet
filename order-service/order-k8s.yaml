apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service-deployment
  labels:
    app: order-service
spec:
  replicas: 2 # Kita suruh Kubernetes menjalankan 2 copy aplikasi biar aman
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        # Nanti akun dockerhub disesuaikan dengan akunmu
        image: kingdil/satset-order-service:latest
        ports:
        - containerPort: 8080 # Ini port asli dari main.go user_service

---

apiVersion: v1
kind: Service
metadata:
  name: order-service # Nama ini yang akan dipanggil oleh pintu masuk (Ingress)
spec:
  selector:
    app: order-service
  ports:
    - protocol: TCP
      port: 80         # Port internal Kubernetes
      targetPort: 8080 # Diarahkan ke port aplikasi Go-mu