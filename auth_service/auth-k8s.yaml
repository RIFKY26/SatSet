apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service-deployment
  labels:
    app: auth-service
spec:
  replicas: 2 # Kita suruh Kubernetes menjalankan 2 copy aplikasi biar aman
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
      - name: auth-service
        # Nanti akun dockerhub disesuaikan dengan akunmu
        image: kingdil/satset-auth-service:latest 
        ports:
        - containerPort: 8081 # Ini port asli dari main.go user_service

---

apiVersion: v1
kind: Service
metadata:
  name: auth-service # Nama ini yang akan dipanggil oleh pintu masuk (Ingress)
spec:
  selector:
    app: auth-service
  ports:
    - protocol: TCP
      port: 80         # Port internal Kubernetes
      targetPort: 8081 # Diarahkan ke port aplikasi Go-mu